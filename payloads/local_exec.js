var shell = WScript.CreateObject("WScript.Shell");
shell.Run("command here");

/*
    Locally execute shell commands on Windows systems, note this will have to override
    index.js (not used in some new version)
*/
